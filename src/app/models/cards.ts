import { Estimations } from './estimations';
import { Product } from './prodcuts';

export interface Card {
  id: string;
  cardId: string;
  customerName: string;
  phoneNumber: string;
  address: string;
}
